version: 2.1

orbs:
  docker: circleci/docker@1.0.1

jobs:
  build-gui-image:
    executor: docker/docker
    steps:
      - setup_remote_docker
      - checkout
      - docker/check
      - docker/build:
          image: respirworks/gui
          dockerfiles: ~/project/gui.Dockerfile

workflows:
  commit:
    jobs:
      - build-gui-image
